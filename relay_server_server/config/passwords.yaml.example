# Copy to passwords.yaml and set real values. Do not commit passwords.yaml.
# Used by the server to connect to database and Redis.

development:
  databasePassword: postgres
  # redis: postgres   # if Redis has a password
