# One row per user: persisted workspace bundle (JSON). Used by WorkspaceRepository.
class: StoredWorkspace
table: stored_workspace
fields:
  userId: int
  data: WorkspaceBundle
  updatedAt: DateTime
indexes:
  stored_workspace_user_id_idx:
    fields: userId
    unique: true
